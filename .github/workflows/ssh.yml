name: SSH Gateway

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest

    steps:

    - name: Checkout
      uses: actions/checkout@v4

    - name: Start tmate session
      uses: mxschmitt/action-tmate@v3
